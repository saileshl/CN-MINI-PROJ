{
  "name": "network-jitter-system",
  "version": "1.0.0",
  "description": "Network Jitter Measurement & Reduction System (All-in-One)",
  "scripts": {
    "start": "node run-all.js",
    "dev": "node run-all.js",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  }
}
